<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Play Store Compliance: Minimal high-risk permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".horisontal_cardv.categories.conjugaison.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        tools:targetApi="34">

        <!-- AdMob Configuration -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />

        <property
            android:name="android.adservices.AD_SERVICES_CONFIG"
            android:resource="@xml/gma_ad_services_config"
            tools:replace="android:resource" />

        <!-- Activities: Standardizing android:exported for API 31+ Compliance -->

        <activity
            android:name=".courslist.CoursListActivity"
            android:exported="false" />

        <activity
            android:name=".courslist.LessonActivity1"
            android:exported="false" />

        <activity
            android:name="com.squareup.leakcanary.internal.DisplayLeakActivity"
            android:exported="false"
            tools:node="merge" />

        <activity
            android:name=".horisontal_cardv.categories.conjugaison.verbeDetails"
            android:label="@string/title_activity_verbe_details"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false" />

        <activity
            android:name=".horisontal_cardv.categories.PhrasesActivity"
            android:exported="false" />

        <activity
            android:name=".horisontal_cardv.categories.subordonnes.subordonnes"
            android:exported="false" />

        <!-- This activity uses a search intent filter, so it must be exported -->
        <activity
            android:name=".horisontal_cardv.categories.conjugaison.Conjugaison"
            android:parentActivityName=".horisontal_cardv.NAVDRAWER"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />

            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
        </activity>

        <activity
            android:name=".horisontal_cardv.categories.Mots.MotsActivity"
            android:exported="false" />

        <activity
            android:name=".recherche"
            android:parentActivityName=".horisontal_cardv.NAVDRAWER"
            android:exported="false" />

        <activity
            android:name=".courslist.DetailsActivity"
            android:parentActivityName=".horisontal_cardv.NAVDRAWER"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false" />

        <activity
            android:name=".Quiz.MainGameActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false" />

        <activity
            android:name=".Quiz.Time_Up"
            android:label="@string/title_activity_time__up"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false" />

        <activity
            android:name=".home"
            android:label="@string/btm"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false" />

        <activity
            android:name=".level3"
            android:label="@string/title_activity_level3"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false" />

        <activity
            android:name=".level2"
            android:label="@string/title_activity_level2"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false" />

        <activity
            android:name=".level1"
            android:label="@string/title_activity_level1"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false" />

        <activity
            android:name=".onboarding.OnboardingActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false" />

        <!-- Main entry point: Must be exported -->
        <activity
            android:name=".splashScreen.SplashScreenActivity"
            android:label="@string/SplashScreenActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".horisontal_cardv.NAVDRAWER"
            android:label="@string/title_activity_navdrawer"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false" />

        <activity
            android:name=".Quiz.PlayAgain"
            android:exported="false" />

        <activity
            android:name=".Quiz.GameWon"
            android:exported="false" />

        <activity
            android:name=".TimeAttackActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false" />

        <activity
            android:name=".FullSimulationActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false" />

        <activity
            android:name=".TcfModuleActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false" />

        <activity
            android:name=".LessonSimulationActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:exported="false" />

        <!-- Receivers: Standardizing android:exported -->

        <!-- Widget Receiver: Must be exported but protected by system-only delivery -->
        <receiver
            android:name=".GrammarWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/rule_widget_info" />
        </receiver>

        <!-- Notification Receiver: Internal only -->
        <receiver
            android:name=".notifications.NotificationReceiver"
            android:exported="false" />

    </application>

</manifest>